.stageFiveSection { margin-top: 18px; }
.patientResponseForm {
  display: grid; gap: 11px; margin-top: 17px; padding-top: 15px;
  border-top: 1px solid var(--line);
}
.patientResponseForm select,
.patientResponseForm textarea {
  width: 100%; display: block; margin-top: 7px; padding: 11px;
  border: 1px solid var(--line); border-radius: 9px; background: white;
}
.followUpList { display: grid; gap: 15px; margin-top: 24px; }
.followUpCard {
  border: 1px solid var(--line); border-left: 5px solid var(--brand);
  border-radius: 14px; padding: 18px; background: #f9fbfb;
}
.followUpCard.priority-priority { border-left-color: #ad6a13; }
.followUpCard.priority-urgent,
.followUpCard.priority-emergency { border-left-color: #a24138; }
.followUpCard h3 { margin: 8px 0 0; font-size: 14px; }
.followUpCard > p { color: var(--muted); font-size: 11px; line-height: 1.6; }
.followUpCard > small { color: var(--muted); font-size: 9px; }
.referralBox, .responseHistory {
  display: grid; gap: 5px; margin-top: 15px; padding: 13px;
  border: 1px solid var(--line); border-radius: 11px; background: white;
}
.referralBox strong, .responseHistory strong { font-size: 10px; }
.referralBox span, .responseHistory span { font-size: 10px; }
.referralBox small { color: var(--muted); font-size: 9px; line-height: 1.5; }
